---
description:
globs:
alwaysApply: false
---

# OCR API Management 项目开发规范

## 1. 项目结构
- 后端代码统一放在 [src/](mdc:src/) 目录，前端代码统一放在 [api-builder/](mdc:api-builder/) 目录。
- 测试代码放在 [tests/](mdc:tests/) 目录，文档放在 [docs/](mdc:docs/) 目录。
- 依赖管理分别使用 [requirements.txt](mdc:requirements.txt)（后端）和 [package.json](mdc:api-builder/package.json)（前端）。

## 2. 后端（Flask）
- 遵循 Flask 工厂模式，使用 Blueprint 组织路由。
- 所有 REST API 路由统一以 `/api/` 开头，返回 JSON 格式数据。
- 推荐使用 Flask-RESTful 进行 API 设计，参数校验、错误处理要规范。
- 数据库操作使用 SQLAlchemy，迁移用 Alembic。
- 所有函数、方法必须添加类型注解和 Google 风格 docstring。
- 严格遵循 PEP8，使用 Black 格式化代码，isort 管理 import 顺序。
- 所有配置通过 [config.py](mdc:src/config.py) 或环境变量管理，敏感信息不得硬编码。
- 测试用 pytest，覆盖所有接口和主要逻辑，测试文件以 test_ 开头。
- 错误处理要统一，返回标准 HTTP 状态码和错误信息。
- 日志记录重要操作和异常，生产环境必须开启 HTTPS。

## 3. 前端（Next.js 15）
- 遵循 Next.js 官方目录结构，组件、hooks、lib、styles 分目录管理。
- 组件必须使用 TypeScript，类型定义完整，props 明确。
- 样式统一用 Tailwind CSS，禁止内联样式。
- API 请求统一封装，所有后端接口通过 `/api/` 路径访问。
- 组件、页面必须有必要的注释和文档，复杂逻辑需单独拆分 hooks。
- 遵循 Prettier/ESLint 规范，保持代码风格一致。
- 静态资源放在 [public/](mdc:api-builder/public/)，全局样式放在 [styles/](mdc:api-builder/styles/)。
- 前端与后端接口数据结构需严格对齐，变更需同步更新。

## 4. 通用规范
- 所有新功能必须配套测试和文档，接口变更需更新 API 文档。
- 严禁提交包含敏感信息的代码，所有密钥、密码等通过环境变量管理。
- 依赖需定期升级，锁定版本，避免安全漏洞。
- 代码提交前必须通过格式化、lint、测试。
- 采用 Git 分支管理，feature/、bugfix/、hotfix/ 命名规范，合并需 Review。
- 重要变更需在 [README.md](mdc:README.md) 或 [docs/](mdc:docs/) 记录。

